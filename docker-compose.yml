version: "3"
services:
  web:
    build: .
    dns:
      - 8.8.8.8
      - 8.8.4.4
    container_name: checkers
    ports:
      - "5000:5000"
    volumes:
      - ./src:/srv/checkers/src
    restart: on-failure
